version: 2.1

orbs:
  go: circleci/go@1.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    executor:
      name: go/default
      tag: "1.14"
    steps:
      - checkout
      - go/load-cache
      - go/test
      - go/save-cache
